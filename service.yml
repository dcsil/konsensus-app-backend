tools:
  exceptions: ""
  logging: ""
  ci: ""

services:
  production: ""

docs:
  frontend: "https://github.com/dcsil/konsensus-app-frontend"
  backend: "https://github.com/dcsil/konsensus-app-backend"

commands:
  bootstrap: "./script/bootstrap"
  database: "mysql.server start"    # must first modify .env file with 
  server: "npm start"
  test: "jest"